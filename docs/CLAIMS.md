# Claim ledger: what is established, by which run, and what is open

Every row names the *shipped* code path that produced the number and the exact
command that regenerates it.  A claim without a matched measurement is listed as
open rather than estimated.  Latency numbers state their measurement window
because the GPU was shared while they were taken.

## A. Established

| # | claim | evidence | command |
|---|---|---|---|
| A1 | Exact Full-KV prefill, then a decode cache compiled to a fixed slot count per (layer, kv-head); decode is exact softmax over the survivors, no parameter added, checkpoint untouched | `qcc_transformer/retention.py`, `docs/REPORT.md` §3.1-3.7 | `pytest -q tests/test_retention.py` |
| A2 | RULER official metric (80 records, 4 tasks) at budget 4096: **shipped-API aggregate retention 1.0071, worst task 1.000**, per-task 1.000/1.000/1.000/1.0283; the harness run agrees (1.0083) | `artifacts/bounded-decode-frontier-multimodel-llama1b.json`, `artifacts/bounded-decode-frontier-ruler-v6.json` | `python benchmarks/analyze_campaign.py multimodel artifacts/bounded-decode-frontier-multimodel-llama1b.json` | **Re-verified on the current code (report 3.42): aggregate 1.0102, worst task 1.0000, 4,608 slots / 144.0 MiB** | `python benchmarks/benchmark_retention_multimodel.py --model /root/qcc/models/Llama-3.2-1B-Instruct --ruler-jsonl /home/waas/ruler_a10_v1/ruler_subset.jsonl --tasks niah_single_1 niah_multikey_2 niah_multikey_3 vt --budget 4096 --lex-cap 512 --hops 6 --obs 64 --nsink 4 --pool 7 --dilate 9 --arms full bounded --out artifacts/bounded-decode-frontier-multimodel-llama1b-recheck.json` |
| A3 | The shipped API reproduces the harness on **all 80 records**: partial and strict recall agree 80/80, 75/80 predictions byte-identical (the other five differ only after the answer, same score), per-task means identical; on the one record where the two implementations differed, the scores are bitwise equal and the selected slot sets identical | `artifacts/bounded-decode-frontier-parity-80.json`, `benchmarks/diff_selection.py` | `python benchmarks/compare_package_to_harness.py --package artifacts/bounded-decode-frontier-multimodel-llama1b.json` |
| A4 | Padded/ragged batches compile per request and merge exactly: per-row slots, values and prefill logits bit-identical to compiling each row alone (including 7,854 duplicated filler slots); a row sliced from the merged cache decodes to the solo tokens | `artifacts/bounded-decode-frontier-batch-validation.json`, §3.19 | `python benchmarks/validate_retention_batch.py` |
| A5 | The law runs on non-Llama checkpoints through one code path: fused `qkv_proj` (Phi-3), per-layer rotary, per-head `q_norm` (Qwen3) | `qcc_transformer/retention.py` helper layer; `benchmarks/smoke_retention_model.py` | `python benchmarks/smoke_retention_model.py <model>` |
| A6 | Chunked prefill must be rope-consistent for LongRoPE checkpoints: without pinning the rotary length, Phi-3.5 scored 0.0 on 16K RULER records because cached keys and observation queries disagreed about position; with `fixed_rope_length` it scores 1.0 | same smoke run; `benchmarks/benchmark_retention_multimodel.py` | `python benchmarks/benchmark_retention_multimodel.py --model <phi> --trust-remote-code --attn-impl eager --prefill-chunk 1024` |
| A7 | Bounded decode state is fixed by configuration, not by prompt length: `budget + lex_cap` slots for every (layer, kv-head, request) | `artifacts/bounded-decode-frontier-state-growth.json` | `python benchmarks/benchmark_state_growth.py` |
| A8 | **Four models, three families, one configuration, no per-model tuning** through the shipped API: Llama-3.2-1B 1.007/1.000, Llama-3.1-8B-4bit 1.014/1.000, Qwen2.5-3B 0.975/0.917, Phi-3.5-mini 0.972/0.889 (aggregate retention / worst task). The two sub-1.0 rows lose only on niah_multikey_3, the task where every model is closest to its own floor; 8B has the strongest Full-KV arm (0.95 there) and is matched exactly | `artifacts/bounded-decode-frontier-multimodel-*.json`, report 3.24 | `python benchmarks/analyze_campaign.py multimodel artifacts/bounded-decode-frontier-multimodel-*.json` |
| A9 | LongBench harness with the official per-task metrics, validated against the official scorer | `benchmarks/longbench_metrics.py`, `tests/test_longbench_metrics.py` | `pytest -q tests/test_longbench_metrics.py` |
| A10 | Matched-budget baselines over the same 80 records: sliding window 0.167, sinks+recent 0.332, last-query 0.842, window-max 0.863, SnapKV-shaped window-mean 0.880, shipped 1.008; only the shipped configuration never fails a task outright | `artifacts/bounded-decode-frontier-baselines-b4096.json`, report 3.21 | `python benchmarks/analyze_campaign.py baselines artifacts/bounded-decode-frontier-baselines-b4096.json` |
| A11 | Language-model NLL under bounded retention is budget- and corpus-dependent, not destroyed: 1.06-1.15x Full-KV below 8K slots, 0.99-1.08x at 8-16K, and on a self-similar corpus slightly *better* at 8K (ppl 0.983x) | `artifacts/bounded-decode-frontier-lm-nll-32k-b{2048,4096,8192}.json`, `artifacts/lm_nll_pinned_b{1024,2048,4096,8192,16384}.json`, `docs/MECHANISM.md` | `python benchmarks/benchmark_bounded_decode_lm_nll.py` |
| A12 | The quality decomposes cleanly (report 3.21): attention ranking alone retains 0.817 aggregate (last query) / 0.870 (window mean); single_1 1.000 and vt 1.039 at 4,608 slots with no anchors; task-agnostic rarity anchors lift it to 0.938; pattern anchors reach 1.0065; chain following adds nothing measurable (shipped 1.0071). Qwen's multikey_3 ratio is 0.833 under three different anchor configurations | `artifacts/bounded-decode-frontier-multimodel-llama1b-{nolex,nolex-mean,rare,rare-hops0,pattern-hops0,mean-anchors}.json` | `python benchmarks/benchmark_retention_multimodel.py --model <checkpoint> --budget 4608 --lex-cap 0 --scoring mean --out artifacts/<name>.json` |
| A13 | LongBench (non-RULER evidence): 9 tasks x 20 records, 122 matched records where Full-KV scores above zero: aggregate retention **1.0049** (macro mean 0.2514 full vs 0.2527 bounded), worst task 0.897 (gov_report ROUGE-L), worst record 0.420; narrativeqa +10.7%, triviaqa/samsum/multi_news/qasper/hotpotqa/2wikimqa/passage_retrieval at parity. Real documents, official per-task metrics, no synthetic needles | `artifacts/longbench-retention-llama32-1b.json` | `python benchmarks/benchmark_retention_longbench.py --model <checkpoint> --tasks narrativeqa qasper hotpotqa 2wikimqa gov_report multi_news triviaqa samsum passage_retrieval_en --limit 20 --budget 4096 --lex-cap 512 --hops 6 --out artifacts/longbench-retention-<name>.json` |
| A14 | At matched decode-state bytes, eviction beats quantization and the two compose (report 3.23): Full-KV int8 is lossless at 2.0x; Full-KV int4 scores 0.4125 partial recall; bounded 4,608 slots bf16 scores 0.8250 at 144 MiB and bounded + int8 reaches Full-KV quality at **72.3 MiB**, 5.9x smaller than the bf16 Full-KV cache | `artifacts/bounded-decode-frontier-kv-quant.json` | `python benchmarks/benchmark_kv_quant_ruler.py --model <checkpoint> --ruler-jsonl <split> --arms full full_int8 full_int4 bounded4096 bounded4096_int8 bounded4096_int4 --out artifacts/bounded-decode-frontier-kv-quant.json` |
| A15 | The residual cross-family shortfall is bounded and characterised (reports 3.24-3.26): the whole bounded/Full-KV difference on `niah_multikey_3` is 4 records on Llama-1B, 0 on Llama-3.1-8B, 3 on Qwen and 2 on Phi, and each is all-or-nothing (recall 0.0 or 1.0), unchanged by five selection configurations, with the failing records self-terminating at 35-83 tokens rather than exhausting the 128-token budget | `artifacts/bounded-decode-frontier-multimodel-*.json` | `python benchmarks/analyze_campaign.py multimodel artifacts/bounded-decode-frontier-multimodel-*.json` |
| A16 | The capacity that binds is the query's, not the context's (report 3.27): both query-driven channels read the last `observation_window` tokens, so the policy serves `min(item names inside the window, item spans the anchor budget reaches)` — measured at 6.0 / 14.3 / 30.7 / 62.3 visible names for `k=64` at `obs` 64 / 128 / 256 / 512, about 8 tokens per named item. The anchor knee sits where the expansion arithmetic predicts (`lex_context_left + lex_context_right + 1 = 49` per item, i.e. 784 at `k=16`): coverage 0.847 with 9.3 of 16 statements retained at `lex_cap 512`, 0.949 with 14.0 at 768, saturating at the window ceiling 14.3; per-item recall follows coverage (0.812 / 0.917 / 0.938) and 4,096 slots at `lex_cap 2048` match the Full-KV arm (0.958) while 8,704 slots at 512 do not (0.896). Widening the window at fixed budget and cap moves both: at `k=32` coverage 0.599 -> 0.973 and recall 0.396 -> 0.833 (above the Full-KV arm's 0.781) from `obs` 64 to 256, and nothing further once coverage reaches 1.000. Retention is necessary but not sufficient: at `k=8` on Llama-3.1-8B-4bit every item statement survives selection in both placements (coverage 1.000, anchor census 8/8) and the arms still differ - 0.625 recall scattered against 1.000 recency - while the final query's attention share on those statements goes from 0.029 to 0.063. With the required set retained by construction (recency placement), 2,048 slots + 512 anchors equal the 32,769-slot Full-KV arm exactly, recall *and* exact-set, on Qwen2.5-3B (`k`=16: 0.875/0.333) and Llama-3.1-8B-4bit (`k`=8: 1.000/1.000, `k`=16: 0.792/0.667) | `artifacts/prediction-knee-k16-lc512-qwen3b.json`, `artifacts/prediction-knee-k16-lc768-qwen3b.json`, `artifacts/prediction-knee-k16-lc1024-qwen3b.json`, `artifacts/prediction-lexcap2048-qwen3b.json`, `artifacts/prediction-recency-qwen3b.json`, `artifacts/prediction-recency-llama8b.json`, `artifacts/prediction-obs64-k16k32-qwen3b.json`, `artifacts/prediction-obs256-k16k32-qwen3b.json`, `artifacts/prediction-mass-llama8b-scatter.json`, `artifacts/prediction-mass-llama8b-recency.json` | `python benchmarks/anchor_window_coverage.py --model <checkpoint> --items 8 16 32 64 --obs 64 128 256 512 --lex-cap 512 2048` and `python benchmarks/analyze_required_set.py artifacts/prediction-knee-*.json` |
| A17 | The worst LongBench task is a budget allocation, not lost content (report 3.28, **re-measured in 3.42**): `gov_report` (median prompt 9,980 tokens) scores 0.829 at 4,608 kept slots, 0.893 at 5,120, 0.941 at 8,704, 0.895 at 9,216 and **1.002 at 16,896 - where 18 of 20 documents fit entirely and the two arms are byte-identical** - with the Full-KV arm at 0.2808 in every row. The loss on the losing records is a degeneration (record 87's bounded summary has a distinct-word ratio of 0.091 against 0.409), which is what 3.28 documented. The 0.897/1.020/1.032 ladder published in 3.28 came from a configuration whose Full-KV arm scored 0.2932 and does not reproduce in this lineage, so the worst-task target is met only when the view holds the whole document | `artifacts/longbench-govreport-{4608,8192,8704,16384}-recheck.json`, `artifacts/longbench-recheck-llama1b.json` | `python benchmarks/benchmark_retention_longbench.py --model /root/qcc/models/Llama-3.2-1B-Instruct --tasks gov_report --limit 20 --arms full bounded --budget 16384 --lex-cap 512 --hops 6 --out artifacts/longbench-govreport-16384-recheck.json` |
| A18 | The shipped configuration is the only policy at Full-KV parity in a head-to-head with the recent eviction families on the same records, budget, sinks, recent window, pooling and decode (report 3.29): on all 80 RULER records - shipped 1.0083 aggregate / 0.500 worst task at 4,608 slots, SnapKV-shaped window mean 0.8804, window max 0.8627, final query only 0.8424, Quest-shaped whole-block selection 0.8316, PyramidKV-shaped layer schedule 0.8289 (1.5x to 0.5x; more state) and 0.8686 (1.25x to 0.75x); StreamingLLM-shaped sinks+recent 0.3319 and sliding window 0.1667 - and every published family fails at least one task outright (worst-task 0.000). On a length-spread 40-record subset the accumulated-attention families are weaker still: H2O 0.7471, TOVA 0.5833 against the shipped 1.0431 with worst task 0.800. Paired per record, the families win 1-6, lose 11-49 and tie the rest | `artifacts/baselines-quest-pyramid.json`, `artifacts/baselines-accumulated.json`, `artifacts/bounded-decode-frontier-baselines-b4096.json` | `python benchmarks/benchmark_bounded_decode_ruler.py --model <checkpoint> --ruler-jsonl <split> --tasks niah_single_1 niah_multikey_2 niah_multikey_3 vt --policies full quest pyramid --budgets 4096 --obs 64 --nsink 4 --pool 7 --dilate 9 --lex-cap 512 --hops 6 --key-chunk 1024 --max-new 128 --out artifacts/baselines-quest-pyramid.json`, then `python benchmarks/analyze_campaign.py baselines --merge artifacts/bounded-decode-frontier-baselines-b4096.json artifacts/baselines-quest-pyramid.json` |
| A19 | Against the industry serving stack at 32K (report 3.30): vLLM 0.11.0 with a paged Full-KV cache decodes 97.9 / 159.8 / 261.9 / 247.6 / 372.7 tok/s at batch 1 / 2 / 4 / 8 / 16 (TPOT 10.2-42.9 ms, decode-only via prefix caching) and its own start-up accounting reports a ceiling of **11 resident 32K requests** at `gpu_memory_utilization=0.6` (11.27 GiB KV) and **17** at 0.85 (17.16 GiB), because a 32K request costs ~1 GiB of KV at 32 KiB/token; the bounded cache (1,152 slots, 36 MiB per request) decodes 70.9 / 142.5 / 291.1 / 583.0 / 1,174.8 / 1,946.8 tok/s at batch 1 / 2 / 4 / 8 / 16 / 32 with recall 1.0 throughout, i.e. it is behind on per-token latency below batch 4 and ahead from batch 8, and it holds 32 resident 32K requests in 1.15 GiB | `artifacts/serving-vllm-32k.json`, `artifacts/serving-vllm-32k-concurrency.json`, `artifacts/bounded-decode-frontier-serving-seq-32k.json` | `python benchmarks/benchmark_vllm_serving_compare.py --model <checkpoint> --length 32768 --batches 1 2 4 8 --max-new 128 --gpu-memory-utilization 0.6 --max-num-seqs 8 --out artifacts/serving-vllm-32k.json` (the script records decode-only throughput by prefilling the prompt once and reusing it through prefix caching) |
| A20 | The published families close the gap on summarisation, where the shipped configuration's anchor channel has nothing to match (report 3.31): on `gov_report` (20 records, 4,096 slots) Quest-shaped block selection reaches 0.2674 against the shipped 0.2649 (full 0.2808), with H2O 0.2636, SnapKV-shaped 0.2561, the layer schedule 0.2533 and TOVA 0.2527; on `samsum` four policies beat Full-KV by 3-7% (SnapKV-shaped 1.073, shipped 1.043, layer schedule 1.030) while the shipped arm leads the task. The seven-policy spread is ~5% on both summarisation tasks against a factor of six on RULER, i.e. the field is bunched where the query names nothing and separated where it does | `artifacts/longbench-published-families.json` | `python benchmarks/benchmark_retention_longbench.py --model <checkpoint> --tasks gov_report samsum --limit 20 --arms full bounded obs_mean quest pyramid h2o tova --budget 4096 --lex-cap 512 --hops 6 --obs 64 --key-chunk 1024 --prefill-chunk 2048 --cache-dir <longbench-cache> --out artifacts/longbench-published-families.json`, then `python benchmarks/analyze_longbench.py artifacts/longbench-published-families.json --view arms` |
| A21 | The filler signal trades retrieval for summarisation (report 3.32): holding the anchor channel fixed, replacing the final-query filler with attention accumulated over the prefill raises the worst summarisation task (`gov_report`, 20 records, 4,096 slots) from 0.2649 to **0.2762** ROUGE-L (ratio 0.943 -> 0.984, full 0.2808) while lowering multi-key retrieval retention on the 40 `niah_multikey_2`/`niah_multikey_3` records from **1.0000 to 0.8929**. Neither filler dominates; the shipped default stays because the aggregate and worst-task requirements are set by the retrieval tasks | `artifacts/longbench-hybrid-filler.json`, `artifacts/baselines-h2o-lex.json` | `python benchmarks/benchmark_retention_longbench.py --model <checkpoint> --tasks gov_report --limit 20 --arms full bounded mean_lex h2o_lex --budget 4096 --lex-cap 512 --hops 6 --obs 64 --key-chunk 1024 --prefill-chunk 2048 --cache-dir <longbench-cache> --out artifacts/longbench-hybrid-filler.json`, and `python benchmarks/benchmark_bounded_decode_ruler.py --model <checkpoint> --ruler-jsonl <split> --tasks niah_multikey_2 niah_multikey_3 --max-records 40 --policies full h2o_lex --budgets 4096 --obs 64 --nsink 4 --pool 7 --dilate 9 --lex-cap 512 --hops 6 --key-chunk 1024 --max-new 128 --out artifacts/baselines-h2o-lex.json` |
| A22 | Low-rank latent KV does not substitute for exact-token selection at matched bytes (report 3.33): on 40 `niah_multikey_2`/`niah_multikey_3` records, the shipped selection keeps 4,608 slots exactly in **144 MiB and retains 1.0000**, an SVD-fitted latent of the whole context at the same 143 MiB (rank 24 of 64) retains **0.2500**, and twice the bytes (259 MiB, rank 38) reaches 0.5357; full rank is exact by construction. The bound is on a *post-hoc training-free* low-rank retrofit, not on MLA-style models that train the projection jointly | `artifacts/lowrank-kv-ruler.json` | `python benchmarks/benchmark_lowrank_kv_ruler.py --model <checkpoint> --ruler-jsonl <split> --tasks niah_multikey_2 niah_multikey_3 --max-records 40 --budget 4096 --lex-cap 512 --obs 64 --nsink 4 --pool 7 --dilate 9 --hops 6 --key-chunk 1024 --max-new 128 --arms full bounded lowrank1 lowrank2 --out artifacts/lowrank-kv-ruler.json` |
| A26 | Blending the two filler signals by **rank** dominates both of its endpoints on the worst summarisation task, and costs nothing measurable on the retrieval slice where retrieval can be read (report 3.41): at 4,608 + 512 slots on Llama-3.2-1B, 20 gov_report records in one invocation give Full-KV 0.2808, `blend_lex` alpha 0.5 **0.2708 (ratio 0.964)**, the accumulated-attention end 0.2669 (0.951) and the window-mean filler 0.2550 (0.912); an alpha sweep bounds the peak (0.957 at 0.35, **0.964 at 0.5**, 0.960 at 0.65, 0.951 at 1.0 - unimodal, so it is not a tuning artefact), and on the 15 shortest `niah_multikey_2/3` records all four policies score the same 0.60 as Full-KV. It still does not meet the 0.97 worst-task bar at this budget (1.020 at 8,704 does) | `artifacts/longbench-worsttask-short-4608.json`, `artifacts/longbench-blend-a0.35-4608.json`, `artifacts/longbench-blend-a0.5-4608.json`, `artifacts/longbench-blend-a0.65-4608.json`, `artifacts/ruler-worsttask-short-4608.json` | `python benchmarks/benchmark_retention_longbench.py --model <ckpt> --tasks gov_report --limit 20 --arms full blend_lex --budget 4608 --lex-cap 512 --blend-alpha 0.5 --out artifacts/longbench-blend-a0.5-4608.json` |
| A23 | The exact 1M prefill runs on a 24 GiB card, and the state rows are measured at both endpoints: 131,072 tokens prefill in **15.0 s** at a 3.05-3.46 GiB peak with bounded **54.0 MiB** (4,632 slots) against Full-KV 1,500.0 MiB (27.8x), and 1,048,576 tokens prefill in **850.65 s** at a **14.06 GiB** peak with bounded **54.0 MiB** against Full-KV **12,288.0 MiB** (**227.6x**) - i.e. **128K -> 1M state growth 1.00x**, the target's ideal value | `artifacts/million-context.json`, report 3.40 | `python benchmarks/benchmark_million_context.py --lengths 131072 1048576 --items 1 --seeds 2 --budget 4096 --lex-cap 512 --prefill-chunk 8192 --value-digits 2 --yarn auto --attn-impl flash_attention_2 --out artifacts/million-context.json` |
| A24 | The 1M row's remaining limit is the *checkpoint*, measured rather than assumed: at 1M both arms score 0 because the exact arm itself fails under **three** rope mechanisms - post-hoc YaRN (`factor=32`) hallucinates the value (asked for `97`, it answers `3. The magic number is 53.`), and both no scaling and dynamic-NTK scaling (`factor=32`) degenerate into repetition (`to the change the change the change`). At 128K, where the model can retrieve, the exact arm is 2 of 2 and the bounded arm 1 of 2. The protocol that makes the row readable at all is part of the measurement: an answer prefix, 2-digit values (every digit is a separate token here), and needle statements that carry their own whitespace | `artifacts/million-context.json`, `artifacts/million-context-noyarn.json`, `artifacts/million-context-dynamic.json`, `artifacts/million-proto-*.json`, report 3.39-3.40 | `python benchmarks/benchmark_million_context.py --lengths 1048576 --items 1 --seeds 1 --arms full bounded --value-digits 2 --yarn dynamic --attn-impl flash_attention_2 --out artifacts/million-context-dynamic.json` |
| A25 | The decode step at batch 1 in eager Hugging Face is host-bound, and the launch-free ratio is measurable once the launch cost is captured away: wall time is flat from 512 to 131,072 cache keys (22.58 / 22.57 / 22.63 / 25.74 ms) while a CUDA-graph capture of the same steps on the SDPA branch gives **3.78 ms at 4,632 keys, 6.84 ms at 131,072 and 23.85 ms at 1,048,576** - i.e. **6.31x at 1M** and 1.81x at 128K at batch 1, rising to 4.65x at batch 8. Capture is refused on the flash decode path, and a profiler kernel sum is not usable as an absolute (it exceeded the wall clock it decomposes), so the probe flags `profile_exceeds_wall` and the graph is the number used | `artifacts/decode-tpot-graph-1m.json`, `artifacts/decode-batch-128k.json`, `artifacts/decode-batch-128k-b16.json`, `artifacts/decode-batch-128k-llama1b.json`, report 3.40 | `python benchmarks/probe_decode_batch.py --contexts 131072 --budget 4632 --batches 1 2 4 8 --steps 12 --out artifacts/decode-batch-128k.json` |

## B. Open questions

| # | open question | what is not established | the experiment that would establish it |
|---|---|---|---|
| B1 | 1M retrieval >= 99.5% | Measured and *not* met, for a reason outside the cache: at 1M the exact Full-KV arm scores 0 by itself on this checkpoint (it hallucinates the value, report 3.40), so no retention policy can reach 99.5% there; the single-needle protocol the checkpoint *can* answer is at parity one length down (128K: exact 2 of 2, bounded 1 of 2). A 0.5B checkpoint with a 12 KiB/token cache is the only 1M-feasible Full-KV configuration on a 24 GiB card | a small-KV checkpoint that is long-context trained (the -1M class of checkpoints does not exist at this size), or a 1M-capable model whose Full-KV cache fits 24 GiB; then re-run the harness with `--items 1 --value-digits 2` |
| B2 | 128K TPOT >= 5x Full-KV | Measured and *not* met on two geometries. Qwen2.5-0.5B (12 KiB/token), launch-free: 1.81x (batch 1), 2.10x (2), 3.03x (4), **4.65x (batch 8)**, infeasible at batch 16 (25.8 GiB of exact cache against a 23.55 GiB card). Llama-3.2-1B (32 KiB/token): 2.16x (batch 1), 3.04x (batch 2), infeasible at batch 4 (16.0 GiB). A heavier KV raises the fixed-batch ratio and reaches the capacity wall sooner, so the batch that would cross 5x is beyond the batch where the exact baseline fits - the constraint is HBM capacity, not the retention law | a card where the exact baseline can be batched past the 5x crossing (or a model whose exact cache stops fitting only after it), measured the same way |
| B3 | Latency percentiles on exclusive hardware | Concurrent load on the shared GPU moves one bounded configuration between 6.9 and 18.4 ms per token, so only p50-style statements with a stated window are supportable | Re-run `benchmark_fullkv_tpot_graph.py` and `benchmark_bounded_decode_tpot_floor.py` on an idle GPU with repeats and report p50/p95/p99 |
| B4 | Finer int4 key quantization, and trained-compression baselines | With `--group-size 128` and a 64-wide head, the key axis uses a single scale per (layer, kv-head), the coarsest KIVI configuration, so the int4 loss in A14 is a property of this configuration rather than of int4; no learned eviction or trained-compression policy has been measured at matched bytes | Per-token or per-channel key scales, and a trained eviction policy, both evaluated at the byte counts in A14 |
| B5 | Bounded prefill transient memory | Retained decode state is bounded, but the exact chunked prefill still holds the prompt's full KV until the compile step, which is `O(L)` for one request | A prefill that keeps a bounded or host-resident KV while producing the same selected slot sets, verified against the current compile on the same records |

## C. What is deliberately not claimed

* That the retention law is a general-purpose attention approximation: it is a
  cache policy, and its quality is task-family dependent (§3.12).
* That language-modelling perplexity is preserved: bounded retention costs NLL
  relative to Full-KV (§3.10), while the retrieval-style tasks above are at
  parity.
* That the RULER retention figure transfers to arbitrary long-context tasks
  without the LongBench run (A13).
* Any 1M *quality* number as an achievement: the 1M retrieval row is measured and
  scores 0 on the exact arm as well (A24), so what is claimed at 1M is the state
  geometry (A23) and the prefill's feasibility, not retrieval quality.
* That the TPOT ratios are a property of the method: at batch 1 in eager Hugging Face
  they measure the runtime's launch overhead (A25); the 5x target is a runtime
  statement and is reported as unmet there.
